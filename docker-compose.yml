services:
  postgres:
    image: postgres:18.3
    restart: always
    environment:
      - POSTGRES_USER=${DB_USER}
      - POSTGRES_PASSWORD=${DB_PASSWORD}
    volumes:
      - postgres:/var/lib/postgresql
    ports:
      - '5432:5432'

volumes:
  postgres:
